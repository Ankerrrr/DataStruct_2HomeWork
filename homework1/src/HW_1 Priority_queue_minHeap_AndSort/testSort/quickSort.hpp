#ifndef QUICKSORT_HPP
#define QUCIKSORT_HPP
#include <vector>
#include <algorithm>

int partition(std::vector<int> &, int, int);
void quickSort(std::vector<int> &, int, int);
void qs(std::vector<int>);

#endif